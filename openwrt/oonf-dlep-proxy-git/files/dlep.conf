[global]
        plugin          dlep_radio
        plugin          layer2info
        plugin          nl80211_listener

[log]
        stderr          false
        syslog          true
#        file            /var/log/dlep.log
#        info            all
#        debug           all

[telnet]
#       port            2009

[dlep_radio=(select outgoing dlep interface)]
        source          wlan0
        not_proxied     false
        proxied         true

[dlep_router=(select incoming dlep interface)]

#[nl80211_listener]
#        if              wlan0
#        interval        1.0

[layer2info]
